def power(x, p):
    return x**p

# Take user input for x and p
x = float(input("Enter the value of x: "))
p = float(input("Enter the value of p: "))

# Calculate x raised to the power of p
result = power(x, p)

# Print the result
print(f"{x} raised to the power of {p} is {result}")
4